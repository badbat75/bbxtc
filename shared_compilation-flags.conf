# Commond C and LD flags
COMMON_CFLAGS="${COMMON_CFLAGS:+"${COMMON_CFLAGS} "}-pipe -g -O3"
COMMON_LDFLAGS="${COMMON_LDFLAGS:+ }"

# Flags to enable LTO
#COMMON_CFLAGS="${COMMON_CFLAGS:+"${COMMON_CFLAGS} "}-flto=${NTHREADS} -frandom-seed=1 -ffat-lto-objects"
#COMMON_LDFLAGS="${COMMON_LDFLAGS:+ }-flto=${NTHREADS} -frandom-seed=1 -fuse-linker-plugin"

# Workaround for the profiledbootstrap profile
#COMMON_CFLAGS="${COMMON_CFLAGS:+"${COMMON_CFLAGS} "}-DHAVE_LIMITS_H -DHAVE_FCNTL_H" # To try to set only on BOOT_CFLAGS

# BOOT_*FLAGS. Check if --with-build-config= is specified.
BOOT_CFLAGS="${COMMON_CFLAGS}"
BOOT_LDFLAGS="${COMMON_LDFLAGS}"

# Workaround for the profiledbootstrap profile
BOOT_CFLAGS="${BOOT_CFLAGS:+"${BOOT_CFLAGS} "}-DHAVE_LIMITS_H -DHAVE_FCNTL_H"

# Enable CCACHE
CC_WRAPPER="ccache"